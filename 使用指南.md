# Hermes Agent 使用指南

> Hermes Agent 是一个自改进的 AI 代理框架，支持多平台消息网关、浏览器自动化、技能系统和多代理协作。

---

## 目录

- [1. 环境要求](#1-环境要求)
- [2. 安装](#2-安装)
- [3. 配置](#3-配置)
- [4. 启动](#4-启动)
- [5. 常用命令](#5-常用命令)
- [6. 工具集](#6-工具集)
- [7. 消息网关](#7-消息网关)
- [8. 常见问题](#8-常见问题)

---

## 1. 环境要求

| 依赖 | 最低版本 | 说明 |
|------|----------|------|
| **Python** | >= 3.11 | 推荐 3.12+ |
| **Git** | 任意 | 版本控制 |
| **Node.js** | >= 20.0.0 | TUI / Dashboard / 浏览器工具（可选） |
| **uv** | 最新版 | Python 包管理器（推荐） |
| **ffmpeg** | 最新版 | 音视频处理（可选） |
| **ripgrep** | 最新版 | 快速文件搜索（可选） |

**支持平台：** Windows（原生，早期 Beta）、Linux、macOS、WSL2、Termux

---

## 2. 安装

### 2.1 快速安装（一键脚本）

```bash
# Linux / macOS / WSL2
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash

# Windows PowerShell（早期 Beta）
iex (irm https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.ps1)
```

### 2.2 手动安装（开发者）

```bash
# 克隆仓库
git clone https://github.com/NousResearch/hermes-agent.git
cd hermes-agent

# 创建虚拟环境并安装依赖
uv venv .venv --python 3.12

# Linux / macOS
source .venv/bin/activate

# Windows
.venv\Scripts\activate

# 安装全部功能 + 开发工具
uv pip install -e ".[all,dev]"
```

> **安装选项说明：**
> - `.[all]` — 包含 CLI、Cron、Web Dashboard、MCP、Home Assistant 等全部功能
> - `.[dev]` — 包含 pytest、ruff、debugpy 等开发工具
> - `.[messaging]` — 仅消息网关相关（Telegram、Discord、Slack 等）
> - `.[voice]` — 语音识别和合成

### 2.3 Docker 安装

```bash
docker compose up
```

> ⚠️ Docker 模式运行在隔离容器中，**无法直接访问宿主机文件和浏览器**。如需本机操作，请使用 uv 安装方式。

---

## 3. 配置

### 3.1 配置文件位置

| 文件 | 路径 | 用途 |
|------|------|------|
| **用户配置** | `~/.hermes/config.yaml` | 所有非敏感设置 |
| **密钥配置** | `~/.hermes/.env` | API 密钥、令牌（仅存放敏感信息） |

> Windows 原生安装时，路径可能为 `%LOCALAPPDATA%\hermes\`。

### 3.2 初始化配置（交互式向导）

> ⚠️ 执行任何 `hermes` 命令前，**必须先激活虚拟环境**：
> ```bash
> # Linux / macOS
> source .venv/bin/activate
>
> # Windows PowerShell
> .venv\Scripts\activate
> ```
> 激活后命令提示符前会出现 `(.venv)` 标识。每次打开新终端都需要重新激活。

```bash
hermes setup              # 完整设置向导（6 个模块）
hermes setup model        # 仅配置模型和提供商
hermes setup terminal     # 仅配置终端后端
hermes setup --quick      # 快速模式
hermes setup --reset      # 重置所有配置
```

向导包含 6 个模块：

1. **模型与提供商** — 选择 AI 提供商和模型
2. **语音合成 (TTS)** — 配置 TTS 提供商和语音
3. **终端后端** — 选择 local / docker / ssh / modal 等
4. **消息平台** — 连接 Telegram / Discord / Slack 等
5. **工具** — 配置搜索、图像生成、浏览器等
6. **代理设置** — 迭代次数、压缩、会话重置

### 3.3 必需的 API 密钥

至少需要配置 **一个** LLM 提供商密钥。在 `~/.hermes/.env` 中添加：

```bash
# === 推荐的 LLM 提供商（选一个或多个） ===

# OpenRouter — 统一接入 200+ 模型
OPENROUTER_API_KEY=sk-or-...

# Google Gemini
GEMINI_API_KEY=AIza...

# xAI / Grok
XAI_API_KEY=xai-...

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# DeepSeek
DEEPSEEK_API_KEY=sk-...

# 阿里云 DashScope（通义千问）
DASHSCOPE_API_KEY=sk-...

# OpenAI 兼容端点（自托管 / 本地）
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=http://localhost:11434/v1   # 如使用 Ollama
```

### 3.4 可选 API 密钥

```bash
# === 搜索工具 ===
EXA_API_KEY=...              # Exa 搜索
TAVILY_API_KEY=...           # Tavily 搜索
FIRECRAWL_API_KEY=...        # Firecrawl 网页提取

# === 图像/视频生成 ===
FAL_KEY=...                  # fal.ai 图像/视频生成

# === 语音 ===
ELEVENLABS_API_KEY=...       # ElevenLabs 高质量 TTS
GROQ_API_KEY=...             # Groq STT（免费额度）

# === 消息平台 ===
TELEGRAM_BOT_TOKEN=...       # Telegram 机器人
TELEGRAM_ALLOWED_USERS=...   # 允许的 Telegram 用户 ID
DISCORD_BOT_TOKEN=...        # Discord 机器人
SLACK_BOT_TOKEN=...          # Slack 机器人
SLACK_APP_TOKEN=...          # Slack App 级令牌

# === GitHub（技能市场） ===
GITHUB_TOKEN=...             # GitHub 访问令牌
```

### 3.5 config.yaml 主要配置段

```yaml
# === 模型配置 ===
model:
  default: "openrouter/google/gemini-2.5-flash"   # 默认使用的模型

# === 代理行为 ===
agent:
  max_turns: 90                  # 最大工具调用迭代次数
  gateway_timeout: 1800          # 网关超时（秒）

# === 终端后端 ===
terminal:
  backend: "local"               # local | docker | ssh | modal | singularity | daytona
  cwd: "."                       # 默认工作目录

# === 上下文压缩 ===
compression:
  enabled: true
  threshold: 0.50                # 上下文使用率达到 50% 时触发压缩

# === 文件检查点（写入前自动快照） ===
checkpoints:
  enabled: false                 # 设为 true 可在文件写入前创建快照
  max_snapshots: 5

# === 显示设置 ===
display:
  compact: false                 # 紧凑模式
  skin: "default"                # 主题: default | ares | mono | slate
  language: "zh"                 # 界面语言

# === 技能系统 ===
skills:
  creation_nudge_interval: 3     # 每隔几轮对话建议创建技能

# === 委派（子代理） ===
delegation:
  max_concurrent_children: 3     # 最大并行子代理数
  max_spawn_depth: 2             # 最大嵌套深度

# === 记忆 ===
memory:
  memory_enabled: true
  user_profile_enabled: true
```

> 完整配置模板参见项目根目录 `cli-config.yaml.example`。

---

## 4. 启动

> ⚠️ **每次使用前必须先激活虚拟环境**，否则会报 `hermes: command not found`：
> ```bash
> # Linux / macOS
> source .venv/bin/activate
>
> # Windows PowerShell
> .venv\Scripts\activate
> ```
> 激活成功后命令提示符前会出现 `(.venv)` 标识。

### 4.1 交互式 CLI（最常用）

```bash
hermes                          # 启动交互式终端对话
hermes --tui                    # 启动 Ink 全屏 TUI 界面
hermes -m "anthropic/claude-opus-4.6"   # 指定模型启动
hermes -q "写一个快速排序"       # 快速提问模式（不进入交互）
```

### 4.2 Web Dashboard

```bash
hermes web                      # 启动 Web 面板（默认 http://127.0.0.1:9119）
hermes dashboard                 # 同上（别名）
hermes dashboard --host 0.0.0.0  # 允许外部访问
hermes dashboard --no-open       # 不自动打开浏览器
```

### 4.3 消息网关

```bash
hermes gateway run               # 启动消息网关守护进程
hermes gateway restart           # 重启消息网关（修改配置后执行此命令生效）
hermes gateway status            # 查看网关状态
hermes gateway logs              # 查看网关日志
```

### 4.4 其他子命令

```bash
hermes setup                     # 运行设置向导
hermes model                     # 切换模型/提供商
hermes tools                     # 管理启用的工具（Curses UI）
hermes sessions                  # 浏览历史会话
hermes logs                      # 查看日志
hermes cron list                 # 查看定时任务
hermes kanban init               # 初始化看板
hermes plugins                   # 查看插件状态
hermes update                    # 更新到最新版本
```

---

## 5. 常用命令

在交互式 CLI 中使用 `/` 前缀的斜杠命令。

### 5.1 会话管理

| 命令 | 别名 | 说明 |
|------|------|------|
| `/new` | `/reset` | 开始新会话 |
| `/history` | — | 查看对话历史 |
| `/save` | — | 保存当前对话 |
| `/retry` | — | 重新发送上一条消息 |
| `/undo` | — | 撤销上一轮对话 |
| `/branch` | `/fork` | 分支当前会话，探索不同路径 |
| `/compress` | — | 手动压缩上下文 |
| `/sessions` | — | 浏览并恢复历史会话 |
| `/resume` | — | 恢复指定会话 |
| `/title` | — | 设置当前会话标题 |
| `/status` | — | 显示会话信息 |

### 5.2 配置切换

| 命令 | 别名 | 说明 |
|------|------|------|
| `/model` | `/provider` | 切换模型 |
| `/yolo` | — | 切换 YOLO 模式（跳过危险命令确认） |
| `/fast` | — | 切换快速模式 |
| `/skin` | — | 切换终端主题 |
| `/verbose` | — | 循环切换工具输出详细度 |
| `/voice` | — | 切换语音模式 |
| `/config` | — | 显示当前配置 |
| `/reasoning` | — | 管理推理力度 |

### 5.3 工具与技能

| 命令 | 说明 |
|------|------|
| `/tools` | 管理工具（列表/启用/禁用） |
| `/toolsets` | 列出所有可用工具集 |
| `/skills` | 搜索、安装、查看和管理技能 |
| `/bundles` | 列出技能包 |
| `/cron` | 管理定时任务 |
| `/browser` | 连接浏览器工具到 Chromium |
| `/plugins` | 列出已安装的插件 |

### 5.4 任务控制

| 命令 | 别名 | 说明 |
|------|------|------|
| `/background` | `/bg` | 后台运行提示词 |
| `/agents` | `/tasks` | 显示活跃的代理和任务 |
| `/stop` | — | 终止所有后台进程 |
| `/queue` | `/q` | 排队等待下一轮执行 |
| `/steer` | — | 在工具调用后注入指令 |
| `/goal` | — | 设置跨轮次目标 |
| `/subgoal` | — | 管理子目标 |

### 5.5 其他

| 命令 | 说明 |
|------|------|
| `/help` | 显示帮助 |
| `/copy` | 复制最后回复到剪贴板 |
| `/paste` | 从剪贴板粘贴图片 |
| `/image` | 附加本地图片到下一条消息 |
| `/usage` | 显示 Token 用量 |
| `/update` | 更新到最新版本 |
| `/quit` | 退出 |

---

## 6. 工具集

Hermes 通过工具集（Toolsets）组织内置能力。每个工具集包含一组相关工具，可按平台独立启用/禁用。

### 核心工具（所有平台共有）

| 工具 | 说明 |
|------|------|
| `terminal` | 在终端中执行命令 |
| `read_file` / `write_file` / `patch` | 文件读写和编辑 |
| `search_files` | 搜索文件内容和文件名 |
| `web_search` / `web_extract` | 网页搜索和内容提取 |
| `browser_navigate` 等 13 个工具 | 浏览器自动化（导航、点击、输入、截图等） |
| `vision_analyze` | 图像分析 |
| `image_generate` | 图像生成 |
| `execute_code` | 运行 Python 代码 |
| `delegate_task` | 派遣子代理并行执行任务 |
| `todo` / `memory` | 任务规划和持久记忆 |
| `clarify` | 向用户提问澄清需求 |
| `send_message` | 跨平台发送消息 |
| `text_to_speech` | 文本转语音 |
| `cronjob` | 定时任务管理 |
| `session_search` | 搜索历史对话 |
| `kanban_*` | 多代理看板协作 |

### 管理工具集

```bash
hermes tools                    # Curses 交互式界面，按平台启用/禁用工具集
```

或在 `config.yaml` 中配置：

```yaml
tools:
  cli:
    enabled: ["browser", "web", "terminal"]
    disabled: ["spotify"]
```

---

## 7. 消息网关

Hermes 支持通过单一网关进程同时连接多个消息平台。

### 支持的平台

Telegram · Discord · Slack · WhatsApp · Signal · Matrix · Mattermost · Email · SMS · 
Home Assistant · DingTalk · 飞书/Lark · 企业微信 · 微信 · QQBot · 
BlueBubbles (iMessage) · 元宝 · Webhook · API Server · Microsoft Teams · Google Chat

### 配置示例（Telegram）

在 `~/.hermes/.env` 中添加：

```bash
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_ALLOWED_USERS=12345678,87654321
```

启动网关：

```bash
hermes gateway run
```

### 多平台同时运行

所有已配置的平台适配器会自动启动，无需额外参数。网关支持：
- **会话隔离** — 每个用户/频道独立会话
- **后台通知** — 长时间任务完成后自动推送结果
- **跨平台消息** — 代理可使用 `send_message` 跨平台发送

---

## 8. 常见问题

### Q: 首次启动应该做什么？

```bash
# 步骤 1：激活虚拟环境（每次打开新终端都需要）
# Windows PowerShell
.venv\Scripts\activate
# Linux / macOS
# source .venv/bin/activate

# 步骤 2：运行设置向导
hermes setup

# 步骤 3：启动交互式 CLI
hermes
```

### Q: 如何切换模型？

在交互式 CLI 中：

```
/model anthropic/claude-opus-4.6
/model openrouter/google/gemini-2.5-flash
```

或使用子命令：

```bash
hermes model
```

### Q: Windows 上 Dashboard 聊天面板无法使用？

Dashboard 的嵌入式 TUI 聊天依赖 POSIX PTY，在原生 Windows 上不可用。解决方案：
- 使用 WSL2 运行
- 或直接使用 CLI 模式（`hermes` 或 `hermes --tui`）

### Q: 如何查看 Token 用量？

```
/usage           # 当前会话用量
/insights        # 使用趋势分析
```

### Q: 如何让代理自动创建和改进技能？

默认启用。代理会在使用过程中自动识别可复用的模式并创建技能。相关配置：

```yaml
skills:
  creation_nudge_interval: 3    # 每隔 3 轮建议创建技能
```

技能存储在 `~/.hermes/skills/` 中，可通过 `/skills` 命令管理。

### Q: 配置文件在哪里修改？

- 非敏感设置 → `~/.hermes/config.yaml`
- API 密钥 → `~/.hermes/.env`
- 运行中重载 `.env`：`/reload`
- 运行中重载 MCP 服务器：`/reload-mcp`

### Q: 如何开启文件写入前的自动备份？

```yaml
checkpoints:
  enabled: true
  max_snapshots: 5
```

开启后，代理每次写入文件前会自动创建快照，可通过 `/rollback` 查看和恢复。

### Q: 如何使用自定义主题？

内置 4 款主题：`default`（金色经典）、`ares`（红色战争）、`mono`（黑白极简）、`slate`（蓝色开发）

```
/skin ares       # 切换主题
```

或自定义：创建 `~/.hermes/skins/<名称>.yaml`，详见 `AGENTS.md` 皮肤系统章节。

---

## 更多资源

- **完整开发文档：** 项目根目录 `AGENTS.md`
- **配置模板：** `cli-config.yaml.example` / `.env.example`
- **贡献指南：** `CONTRIBUTING.md`
- **项目仓库：** https://github.com/NousResearch/hermes-agent
